extends layout

block content


    .container
        h2.text-center.text-primary= "Please select document for uploading:"

        form.navbar-form.navbar-left(ref='uploadForm' id='uploadForm' action='/files/upload' method='post' encType="multipart/form-data")
            .form-group
                input.form-control#exampleInputFile(name="foo" type="file")
            button.btn.btn-primary(type='submit') UPLOAD