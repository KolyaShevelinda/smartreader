extends layout

block content


    .container
        h2.text-center.text-primary= "Please enter search criteria:"

        form.navbar-form.navbar-left(role='search', action='/documents/all' method='get')
            .form-group
                input.form-control(type='text', placeholder='Search', name="search")
            button.btn.btn-default(type='submit') Submit